let numbers = [1,2,3,4];

// numbers.splice(0);

// numbers.forEach((num) => numbers.shift())

/*
numbers.forEach(function(num) {
  numbers.shift();
});
*/

console.log(numbers);


/*
let i;

for(i = 0; i < 4; i += 1) {
  numbers.splice(0,1);
}
*/